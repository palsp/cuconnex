import { Request, Response, NextFunction } from 'express';


// Always declare business logic in controller file
// Always declare return type
export const businessLogic = async (req: Request, res: Response, next: NextFunction): Promise<void> => {

}